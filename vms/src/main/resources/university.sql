CREATE TABLE university (
    school_code BIGINT PRIMARY KEY,
    name VARCHAR(100),
    level VARCHAR(50),
    region VARCHAR(50),
    location VARCHAR(50),
    score_line INT,
    website_link VARCHAR(200)
);